<Window x:Class="session7_projects.VisibilityExample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:session7_projects"
        mc:Ignorable="d"
        Title="VisibilityExample" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="158*"/>
            <RowDefinition Height="161*"/>
        </Grid.RowDefinitions>
        <StackPanel Height="75" Background="Orange" Grid.Row="0">
            <!--The first button is Collapsed so it does not take up any space-->
            <Button Visibility="Collapsed">Collapsed Button</Button>
            <Button>This button is below a collapsed button</Button>
        </StackPanel>
        <StackPanel Height="75" Background="Orange" Grid.Row="1">
            <!--The first button is hidden it does not appear but still it takes space-->
            <Button Visibility="Hidden">Hidden Button</Button>
            <Button>This button is below a hidden button</Button>
        </StackPanel>
    </Grid>
</Window>
